# TDD Evidence Log

This log records observable RED and GREEN evidence for each production behavior change. A test that first appears after implementation does not count as TDD and earns no scorecard discipline credit.

| Task | Behavior | RED command and observed failure | GREEN command and observed result | Commit |
|---|---|---|---|---|
| Task 2 | Case-sensitive canonical firmware build | `arduino-cli compile -b esp32:esp32:esp32s3 --board-options USBMode=default .` (2026-07-11) -> exit 1, `missing .../BP_checker.ino` | `arduino-cli compile .` -> exit 0, flash 1,078,376 bytes (82%), globals 48,540 bytes (14%) | `3c02f84` |
| Task 2 | `VERSION` must not shadow C++ `<version>` on case-insensitive filesystems | First `bash scripts/run_quality_gate.sh` -> host compile read `./version:1` as a header and failed with 19 errors | `bash scripts/run_host_tests.sh` after using `-iquote .` -> 175 checks pass | `3c02f84` |
| Task 2 | Pinned warning-audited build and traceable SBOM | First full firmware gate -> project warnings for ArduinoJson v7 APIs, storage key truncation, and USB config initialization; malformed quoted flags also warned | `bash scripts/run_quality_gate.sh` -> exit 0, zero project warnings, firmware 1,078,544 bytes, clean source `3c02f84`, artifact SHA-256 `4c4dc4961a7caa04ff43e3b9f7926f7bc342a2481f51345d3637b24907278dab` | `3c02f84` |
| Task 2 review | Non-allowlisted compiler/project warnings fail closed | First `bash test/tooling/test_build_contract.sh` -> exit 1 because no path-aware warning checker existed; review also showed `cc1plus: warning:` bypassed the old repository-path grep | `bash test/tooling/test_build_contract.sh` -> pass; pinned core paths are allowed while compiler/project fixtures are rejected | `62f8487` |
| Task 2 review | SBOM covers every linked dependency and requires Git identity | `jq -e '.components[] | select(.name == "espressif_usb_host_cdc_acm")' build/sbom.json` -> exit 4; standalone generator accepted fallback `unknown` | Clean gate SBOM contains Apache-2.0 vendored CDC snapshot SHA-256 `51c3b1721c085270058269d434edb36fdb9f92484fbbd91b4b05cbe50d8669d3`; invalid `GIT_DIR` exits 128 | `62f8487` |
| Task 2 review | Artifact embeds exact version and reviewed source SHA without replacing platform flags | `strings build/firmware/BP_checker.ino.bin` found neither identity; initial `build.extra_flags` override inflated firmware to 1,106,856 bytes | Clean `bash scripts/run_quality_gate.sh` embeds `0.1.0-dev` and full `62f8487...` via `compiler.cpp.extra_flags`; firmware 1,078,704 bytes, SHA-256 `b73faa81aba0599dda734efabddbf88d7454555d3edf81f83df954792ff2cad1` | `62f8487` |
| Task 3 | Exact HBP-9030 format-5 grammar, ranges, device error, movement, and transient identity | First `bash scripts/run_host_tests.sh` after adding the protocol corpus -> exit 1, `fatal error: 'lib/BPProtocol.h' file not found` | `test_hbp9030_protocol` passes 59 initial checks; the migrated parser/data suites reject the former permissive fixtures | `1908154` |
| Task 3 | HBP formats 1-4 fail with an actionable unsupported-format result | Added format probes before the result existed; host compile -> exit 1, `BPParseError` has no member named `UNSUPPORTED_FORMAT` | `test_hbp9030_protocol` passes 65 checks, including all three legacy wire signatures and unsupported models | `1908154` |
| Task 3 | Strict parser remains warning-clean in the pinned firmware build | First full `bash scripts/run_quality_gate.sh` -> exit 1 because timestamp `snprintf` could truncate under `-Wformat-truncation` | Clean `bash scripts/run_quality_gate.sh` -> 210 host checks and UI checks pass; firmware 1,077,956 bytes, artifact 1,078,112 bytes, clean source `1908154`, SHA-256 `598f225292bff87104ffd1a591f263cbc8a86af652155968aac8cfe6ba8ea1eb` | `1908154` |
| Task 3 review | Subject ID and timestamp construction fail closed on `String` exhaustion | Allocation-fault injection at each of four `reserve`/`concat` sites -> 8/73 protocol assertions failed because the parser still returned a valid measurement | `bash scripts/run_quality_gate.sh` -> 218 host checks and UI checks pass; clean source `f3dcfcb`, firmware artifact 1,078,432 bytes, SHA-256 `46baaf5f082cfd3d755686d2da1e6fc3b9dee8886048434b401d02d04492a23c` | `f3dcfcb` |
| Task 4 | Device time, exact CRLF/no-idle framing, sanitized reasons, and identity-free diagnostics | Focused `test_data_processor` run after adding clock/Serial instrumentation -> 15/45 assertions failed: system time replaced device time, partials idle-flushed, LF/CR-only input persisted, and raw ID/hex reached diagnostics/records/logs | `bash scripts/run_host_tests.sh` -> 253 checks pass; partials survive 5 seconds, only CRLF completes, device time/source persist, and leak markers are absent | `c93b779` |
| Task 4 | Raw-frame record and Web sinks are structurally removed | First `bash scripts/check_ui_markup.sh` after changing the privacy contract -> exit 1, `missing token: diagnostic-data`; old `/raw_data` and `rawData` paths remained | UI check passes with `diagnostic-data` required and identity-bearing route/field tokens forbidden; firmware compiles without `BPData::rawData` | `c93b779` |
| Task 4 | Transport loss is ordered with bytes and invalidates through a trusted boundary | Focused host compile after adding an in-band loss test -> 9 errors including unknown `MonitorRxEvent` and no overridable `dataLossCount()` | DataProcessor consumes ordered POD byte/discontinuity events; loss-spanning bytes are discarded through CRLF and the next clean frame persists | `c93b779` |
| Task 4 | Verified fixed-length framing handles bursts, fragments, embedded LF, and resynchronization | Focused framer compile -> exit 1, no member named `fixedLengthVerified` | `test_protocol_framer` passes 12 checks; ASan/UBSan and `-Werror` focused runs pass on macOS (leak sanitizer unsupported) | `c93b779` |
| Task 4 | Warning-clean traceable target build after privacy/framing changes | Pre-commit host/UI/sanitizer runs were green but no clean artifact existed for the new behavior | Clean `bash scripts/run_quality_gate.sh` -> 253 host checks and UI checks pass; firmware 1,062,928 bytes, artifact 1,063,072 bytes from `c93b779`, SHA-256 `a5db432637cd6ac4d75832e273592597b95a14494cf77ed1afc04da1702c0061` | `c93b779` |
| Task 4 review | Framing contracts fail closed at arithmetic/capacity edges | New `SIZE_MAX` line and sync-only fixed probes -> 4/17 framer assertions failed; the line contract retained a byte and fixed completion was skipped | `test_protocol_framer` passes 17 checks under `-Werror` + ASan/UBSan; capacity is compared without overflowing and sync-only frames complete in-bounds | `9d4197d` |
| Task 4 review | A trusted reconnect reset accepts the first clean new frame | Focused DataProcessor compile -> exit 1, `MonitorRxEventType` had no `STREAM_RESET` | DataProcessor distinguishes mid-frame loss (`DISCONTINUITY`, discard through CRLF) from an ordered clean reset (`STREAM_RESET`, clear then accept); 55 checks pass | `9d4197d` |
| Task 4 review | Transient subject ID bytes are overwritten on valid and error destruction | Destructor-wipe probes -> 2/77 assertions failed with zero cleared bytes | Move-only `BPParseResult` securely overwrites the transient String before release on every result path; 77 protocol checks pass | `9d4197d` |
| Task 4 review | Encoded identity canaries cover diagnostic, Serial, NVS, and CSV sinks | Expanded sink test initially failed to compile because fake NVS had no whole-store substring probe | Valid and device-error probes reject plaintext, lower/upper hex, Base64, and percent forms across sinks; UI static gate forbids transient/raw fields | `9d4197d` |
| Task 4 review | Review fixes remain warning-clean and commit-traceable | Initial Task 4 artifact predates the bounds, reset, and secure-wipe review fixes | Clean `bash scripts/run_quality_gate.sh` -> 293 host checks and UI checks pass; firmware 1,062,884 bytes, artifact 1,063,040 bytes from `9d4197d`, SHA-256 `6569ce3faa2ee6e1c1497be2436cc1077194fb48b977a3ffa6f9eed4617d76dc` | `9d4197d` |
| Task 5 | Pure USB lifecycle reducer covers install/open/config/error/disconnect/overflow/retry | First focused compile -> exit 1, `lib/transports/UsbCdcState.h` not found | Initial reducer passed 50 checks; stale-result, duplicate-terminal, close-pending retry, and recovery review probes then produced 16/86 failures before the guarded state machine passed | `8c31264` |
| Task 5 | Ordered byte-boundary controls survive saturation, sessions, and sequence wrap | Cursor tests first produced 17 missing-type/member compile errors; next-session transition then failed 2/99 assertions | Reducer/cursor suite passes 98 checks including exact boundary ordering, prior-session rejection, next-session reset, wrap, reserved critical capacity, and exact loss counters | `8c31264` |
| Task 5 | CDC callbacks are POD-only and main exclusively owns String/handles | First `bash scripts/check_usb_callback_contract.sh` -> exit 1, no balanced `CALLBACK_POD` blocks; legacy callbacks mutated String/state/handles | Static gate passes: callback blocks use zero-timeout stream/queue writes and contain no String/state/close/delay; volatile ring ownership is absent | `8c31264` |
| Task 5 | Producer/consumer ordering is executable under concurrency instrumentation | No executable concurrency harness existed for separate byte/control channels | 200,000-byte/201-control SPSC boundary stress passes normally and under ThreadSanitizer; focused reducer ASan/UBSan + `-Werror` passes | `8c31264` |
| Task 5 | Task-safe USB changes remain warning-clean and commit-traceable | Pre-commit host/static/TSan/firmware runs were green but no clean source artifact existed | Clean `bash scripts/run_quality_gate.sh` -> 391 host checks, concurrency/static/UI gates, and firmware build pass; artifact 1,070,272 bytes from `8c31264`, SHA-256 `14edfd687591d21a9d1034dd0deb90b0648eaa84babe53ae9e65a5bc68406c74` | `8c31264` |
| Task 5 review | CDC handles, transfers, callback arguments, and driver objects remain alive through close/uninstall races | Independent review found caller-notification reuse, timed-out close ownership, control/OUT reuse, failed-open cleanup, raw-handle admission, DEV_GONE synthetic completion, and driver-object deletion races | Request-owned close completion, generic/control/OUT leases, immutable callback contexts, real callback drain, stable lifetime serialization, and retryable teardown pass the static ownership gate, 305 ASan/UBSan checks, TSan stress, and independent driver/transport re-review with no P1/P2 | `8e21c5a` |
| Task 5 review | Queue saturation preserves byte ordering, exact loss diagnostics, and the strongest terminal fact | Review probes exposed late rejected bytes missing from totals, ERROR/DISCONNECTED type loss on a full lifecycle queue, and mixed-generation atomic diagnostics | One mutex owns epochs/counters/snapshots; typed terminal replay upgrades ERROR to DISCONNECTED; ordered quarantine barriers and failed-open reaping pass deterministic reducer tests plus six concurrency scenarios | `8e21c5a` |
| Task 5 review | Initial/periodic poll failures and hostile CDC notifications fail closed without identity-bearing dumps | Short/misaligned notifications could read out of bounds; pre-publication and resubmit failures could leave READY without polling; unsupported notifications reached a raw hex log | Length/type/interface parsing is byte-safe, every terminal poll failure is latched or reported, malformed notifications stop polling, and the static privacy gate forbids raw notification-buffer logging | `8e21c5a` |
| Task 5 review | Stress completion observes producer completion before the final synchronized pending snapshot | TSan fallback run exited one publication early: `drops=99999` of 100000 because the harness sampled `pending=0` before acquire-loading `producersDone=2` | Done-first termination passes repeated normal runs, 61 investigation TSan runs, and the clean full-gate TSan run; production channel code was unchanged for this harness defect | `8e21c5a` |
| Task 5 review | Reviewed USB snapshot is warning-audited, traceable, and independently accepted | Earlier artifacts predated final lifetime, notification, teardown, and privacy fixes | Clean `bash scripts/run_quality_gate.sh` -> 598 host checks, static/UI gates, six concurrency scenarios and TSan pass; firmware artifact 1,080,304 bytes from clean source `8e21c5a`, SHA-256 `00f66e7e9b576e25e6a5f2f0646cfd5b794df2f86835a8ea0260d5d8edeafa37` | `8e21c5a` |
| Task 6 | Byte-defined v3 state/slots reconstruct history without count/index metadata | Initial persistence RED compiled but failed 66/183 checks; the expanded `-Werror` + ASan/UBSan suite failed 153/486 checks across API types, LE/CRC vectors, slot corruption, 64-bit sequences, migration, and tombstones. The old zero-capacity implementation also triggered sanitizer division-by-zero and heap-overflow diagnostics | Focused `test_record_manager` passes 1,199 checks under `-Werror` + ASan/UBSan; state/slot golden vectors, every truncation, seeded bit flips, valid-CRC semantic corruption, duplicate rejection, wrap, exhaustion, and startup reconstruction pass | `6cbbcd6` |
| Task 6 | Append, migration, and clear remain old-or-new at every write/power-cut boundary | The one-shot fault fake could continue writing after the simulated cut; expanded HARD_CUT before/after matrices exposed non-atomic v2 writes, hybrid full-ring state, and missing activation/tombstone behavior | Latched hard-cut matrices cover append/full-ring overwrite and every v2/legacy migration and clear ordinal; one binary slot write appends, migration activates `v3_state` last, and clear commits its generation first | `6cbbcd6` |
| Task 6 review | Degraded or under-reported legacy sources never become a lossy authoritative v3 set | Reboot/source-retention probes failed 5/1,147 checks because valid survivors were activated and bad source keys deleted; extra-key probes then failed 4/1,153 because torn count/index metadata could under-report durable records | Malformed, missing, and unexpected v2/legacy keys remain authoritative, keep validated survivors diagnostic-only, create no `v3_state`, and block mutations until repaired or safely reset; focused suite passes 1,199 checks | `6cbbcd6` |
| Task 6 | Measurement and clear success are reported only after durable storage success | Focused DataProcessor RED failed 11/95 checks because accepted HTML/logging preceded persistence; UI static RED lacked a clear-failure contract; CSV integration initially failed 4/6 after strict timestamp provenance | DataProcessor passes 99 checks and tells operators to inspect history before retrying an ambiguous write; clear preserves diagnostics and returns 503 on failure; UI/static and CSV 6/6 checks pass | `6cbbcd6` |
| Task 6 review | Transient cleanup faults recover in-process and boot storage failure is visible | Same-process cleanup retry probes failed 10/1,199 because `_storageHealthy=false` was sticky; the startup contract gate exited 1 because the sketch ignored `loadFromStorage()` and always claimed success | Add/clear reconcile unhealthy state before retry; startup renders sanitized `storage_error`, logs `history_load_failed`, and returns before the success claim; independent manager/integration re-review reports no P1/P2 | `6cbbcd6` |
| Task 6 review | Reviewed persistence snapshot is warning-audited and traceable | Pre-commit gates were green but could not provide a clean-source artifact for the final review fixes | Clean `bash scripts/run_quality_gate.sh` -> 1,770 host checks, USB static/stress/TSan, UI, firmware, and SBOM pass; firmware 1,086,608 bytes (82%), artifact 1,086,752 bytes from clean source `6cbbcd6`, SHA-256 `4ab4bca52314aa12090ae98eaf2bd291fa276024311bae01f2a86e10c35d30ce` | `6cbbcd6` |
| Task 7 | Atomic claim state and independent AP/bootstrap/admin/staff secrets | First focused security compile after adding the contract -> exit 1 because `lib/DeviceSecurity.h` did not exist | Fault-injected security suite passes 1,653 checks, including entropy failure, every persistence cut, one-time claim, rotation, recovery, and wipe-pending boot states | `d469a65` |
| Task 7 | Request parsing, policy, form input, response output, and socket work are bounded before handlers | The initial runtime contract rejected the stock `WebServer` path and the focused suites first lacked the bounded request/response/transaction/runtime types | Bounded request 3,462, response 123, transaction 111, socket 25, and form 1,054 checks pass; static runtime contract rejects stock parsing and blocking response paths | `3aa32df` |
| Task 7 | Claimed onboarding and physical recovery preserve the default-deny role boundary | New gate probes first denied authenticated admin configuration on the just-claimed provisioning AP; recovery probes then failed 2/227 policy and 1/2,191 gate assertions | Policy passes 233 checks, request gate passes 2,191, and the pure network lifecycle passes 16 checks for STA-only loss behavior plus exact/wrap-safe 10-minute recovery expiry | `99b829d` |
| Task 7 review | Boot allowlist, live AP rotation, driver failure cleanup, and recoverable onboarding match product claims | Focused compile -> 6 undeclared `credentialRotationRequiresRestart` errors; integration contract exited 1 for the missing boot allowlist, AP cutover, RAM wipe, and recovery guidance | `test_web_access_policy` passes 233 checks; security integration/UI contracts pass; active-AP secret rotation defers reboot, unsupported stored models fall back safely, and onboarding retains the AP/recovery credentials required to recover | `99b829d` |
| Task 7 | Reviewed security/network snapshot is warning-audited and commit-traceable | Pre-commit builds embedded the prior `3aa32df` source identity and therefore were not evidence for the integrated commit | Clean `bash scripts/run_quality_gate.sh` at `99b829d` passes all host/static/stress/TSan/UI/build/SBOM stages; sketch 1,115,128 bytes (85%), artifact 1,115,280 bytes, globals 72,404 bytes (22%), SHA-256 `6f26f84b8cba983ad39ecad87a2f7684308ec3120261970443201c0523f75116` | `99b829d` |
| Task 8 | Configurable review/urgent policy, invalid values, freshness transitions, persisted-history provenance, ring-wrap revision, and repeat guidance | `c++ -std=c++17 -Wall -Wextra -iquote . -Itest/host -o build/host_tests/test_measurement_policy test/host/test_measurement_policy.cpp` -> exit 1, `fatal error: 'lib/MeasurementPolicy.h' file not found` | Same focused compile with `-Werror`, then `build/host_tests/test_measurement_policy` -> 55 checks pass; focused `test_record_manager` remains 1,199/1,199 | `02ff21b` |
| Task 8 | Current AHA wording keeps the configured 130/80 review boundary separate from clinic-defined pulse rules and reserves the urgent cue for values above 180/120 | Focused policy run after tightening the official boundary -> exit 1, 2/52 checks failed: 180/120 was marked urgent and the policy name did not separate pulse attribution | Focused policy run -> 52 checks pass before the later opaque-sequence cases expanded it to 55 | `02ff21b` |
| Task 8 | Safe clinical markup, role-separated admin discovery, whole-surface revision refresh, and visible poll failure | First `bash scripts/check_ui_markup.sh` after adding the static contract -> exit 1, `missing token: :focus-visible` | `bash scripts/check_ui_markup.sh` -> exit 0, including language, focus, live status, table, mobile overflow, no diagnostic/identity copy, admin guards, API state, revision reload, and poll-failure contracts | `02ff21b` |
| Task 8 review | Opaque `uint64_t` revision/record/session values remain exact above JavaScript's 2^53 safe-integer limit | Focused compile -> exit 1 with 2 undeclared `formatOpaqueSequence` errors; UI gate -> exit 1 because `setUInt64Json(doc["revision"], recordManager->getRevision())` was missing | Focused policy run -> 55 checks pass including exact `UINT64_MAX` decimal text and truncation rejection; UI gate requires all five JSON writes to use decimal strings and forbids numeric assignments | `02ff21b` |
| Task 8 | Integrated policy, persistence, receive path, staff/admin boundary, and static UI snapshot | New behavior had only focused GREEN evidence before integration | Owning run: policy 55, record manager 1,199, DataProcessor 99, Web access policy 233, and UI markup all pass; `bash scripts/run_host_tests.sh` passes every host executable | `02ff21b` |
| Task 8 | Warning-audited firmware and the complete repository gate remain green | Focused host/UI/compile runs did not cover the concurrency stress, TSan, every static contract, or SBOM stage together | `bash scripts/run_quality_gate.sh` -> exit 0; all host/static/stress/TSan/UI/build/SBOM stages pass, sketch 1,123,468 bytes (85%), artifact 1,123,616 bytes, globals 72,420 bytes (22%), SHA-256 `1b983392394e304d002b02ddb11bd8e9ba2e2c14b6ffdf72357e8108bff9f46b` | `02ff21b` |
| Task 8 spec fix | Main-loop-owned 64-bit uptime keeps stale state and receive age monotonic across repeated `millis()` wraps | Focused compile -> exit 1: 2 `unknown type name 'MonotonicMillis64'` errors, `uint64_t` age could not bind to the old `uint32_t&`, and a >2^32 timestamp produced a truncation warning; UI static -> exit 1, missing `MonotonicMillis64 uptimeClock` | Focused policy suite first passed 61 checks; final suite passes 149 checks. Sketch observes the clock before `server.handleClient()` on every loop and injects it into persistence/Web | `595ea60` |
| Task 8 spec fix | Named/versioned administrator policy is one validated, checksummed, crash-consistent NVS image | Focused compile -> exit 1 with missing `MeasurementPolicyStore`, `MeasurementPolicyResult`, name/version, and update APIs; boot static -> exit 1, missing global store/load contract | Final policy suite passes 149 checks: exact 77-byte image, one-write create/update, round-trip, strict form syntax, invalid/no-write, type/length/CRC corruption, NVS begin failure, and BEFORE/AFTER/HARD_CUT old-or-new recovery | `595ea60` |
| Task 8 spec fix | Locked/corrupt policy state cannot accidentally expose a usable factory fallback | Focused policy run -> exit 1, 2/149 checks failed because corrupt and storage-failed stores still returned a semantically valid default config | Focused policy run -> 149 checks pass after locked config is explicitly invalidated | `595ea60` |
| Task 8 spec fix | Staff non-discovery and direct denial share executable role/route policy | Focused access compile -> exit 1: route count remained 16 instead of 18 and `WebSurface`/`surfaceVisible` were undeclared; UI static -> exit 1, missing `/measurement_policy` | Access policy passes 276 checks across every staff/admin surface and all 18 routes; request gate passes 2,201 checks and keeps 512-byte bounded admin mutation/no-store/default-deny behavior | `595ea60` |
| Task 8 spec fix | Live operations counters, active policy injection, and history timestamp provenance match rendered UI/API | UI static initially failed on missing policy route, then on the remaining implicit `classifyMeasurement(latest)` default | UI static passes with active persisted policy at every classify/freshness site, live `data-loss-count`/`reconnect-count` DOM updates, and scoped history `時間來源`; firmware compiles warning-gated | `595ea60` |
| Task 8 spec fix | Reviewed fix snapshot is warning-audited and commit-traceable | Focused tests did not jointly cover all host, static, stress, TSan, firmware, and SBOM stages | Clean `bash scripts/run_quality_gate.sh` at `595ea60` passes; sketch 1,131,252 bytes (86%), artifact 1,131,408 bytes, globals 72,524 bytes (22%), SHA-256 `82c1b488d1c4dd490f1d827a04df929ea0a2361898b7032dfb3d94098c3d5e5e` | `595ea60` |
| Task 8 spec re-review | A policy-only update cannot leave dashboard policy labels and recent-row classifications inconsistent with live KPI colors | New `bash scripts/check_ui_markup.sh` contract -> exit 1, `missing policy-version dashboard reload contract: html += "';let bpPolicyVersion='";` | Dashboard snapshots `activePolicy().policyVersion` as decimal text and compares `String(d.policy_version)` immediately after `/api/latest` parsing, before poll state or DOM updates; focused UI/static and every host executable pass | `cf30577` |
| Task 8 final | Policy-version reload fix is warning-audited and commit-traceable | Focused UI/static and host runs did not jointly cover USB stress, TSan, firmware, and SBOM stages | Clean `bash scripts/run_quality_gate.sh` at `cf30577` passes; sketch 1,131,380 bytes (86%), artifact 1,131,536 bytes, globals 72,524 bytes (22%), SHA-256 `68e272cdb09bc0b21a6f650f09ceccc1c9aa0d0ebbce9611f66233bf1aceb58e` | `cf30577` |
| Task 8 important fix | A blackholed `/api/latest` request cannot freeze dashboard freshness indefinitely | New `bash scripts/check_ui_markup.sh` contract -> exit 1, `missing bounded dashboard polling/watchdog contract: AbortController`; threshold separation then exited 1 on missing `responseAge>=bpRequestDeadlineMs` | UI/static passes with one serialized poll, an 8-second abort deadline, an independent 1-second watchdog, active-policy record staleness, visible disconnect/action copy, and page-exit cleanup; every host executable passes | `a58ae2e` |
| Task 8 minor fix | Exhausted policy versions do not wrap the administrator update form to version zero | Pre-fix page unconditionally rendered `policy.policyVersion + 1U`; the new UI/static contract requires an explicit `UINT32_MAX` branch | At `UINT32_MAX`, the update form is replaced by a controlled-maintenance explanation that the policy cannot be updated in place | `a58ae2e` |
| Task 8 important fix | A dashboard restored from the browser back-forward cache cannot remain permanently stopped after `pagehide` cleanup | New `bash scripts/check_ui_markup.sh` contract -> exit 1, missing `window.addEventListener('pageshow',(event)=>{if(event.persisted)location.reload();})` | Persisted `pageshow` forces a clean reload; ordinary initial `pageshow` does nothing, preventing a reload loop. UI/static and every host executable pass | `a5fd37e` |
| Task 9A | Signed release metadata, bounded artifact verification, pending-health confirmation, and monotonic anti-replay state fail closed | Before production code existed, `c++ -std=c++17 -Wall -Wextra -iquote . -Itest/host -o build/host_tests/test_firmware_update_policy test/host/test_firmware_update_policy.cpp` -> exit 1, `fatal error: '../../lib/FirmwareUpdatePolicy.h' file not found` | Focused `-Werror` and ASan/UBSan runs pass 90 checks; `bash scripts/run_host_tests.sh` passes every host executable including malformed/canonical manifests, missing/wrong trust, wrong target/hash/size, interrupted stream, every two-slot cut, reset invariance, rollback health, replay, downgrade, and exhaustion | `39c14fd` |
| Task 9A review | Only signature-authorized metadata can stream or enter pending boot; corrupt monotonic state and abandoned OTA sinks fail closed | Review probes failed 3/96 checks: failed `begin()` did not clean the sink, scoped destruction did not abort, and a corrupt newest slot loaded an older replayable sequence. API inspection also showed unrestricted parsed `Manifest` could start streaming | Focused `-Werror` + ASan/UBSan passes 106 checks; `AuthorizedManifest` is produced only after signature/target/sequence verification and is bound to the current anti-replay sequence, every present corrupt slot locks storage, and RAII abort covers failed begin/destruction | `f9d4deb` |
| Task 9A re-review | An ambiguous post-write read cannot leave a stale in-memory sequence authorized for another update | Applied-write/read-failure probe failed 3/112 checks: store remained ready, accepted a lower follow-on sequence, overwrote the already-applied higher slot, and reboot observed 8 instead of 9 | Focused `-Werror` + ASan/UBSan passes 112 checks; any failed or mismatched reconciliation zeroes and locks runtime state until an explicit successful reload observes the durable old-or-new slot | `b2bf543` |
| Task 9C | A bounded external stream sink owns begin/write/finish/abort exactly once without buffering the body | Before production code, focused compile exited 1 with `fatal error: '../../lib/BoundedStreamConsumer.h' file not found` | Focused `-Werror` and ASan/UBSan pass 67 checks; missing callbacks, zero/over/underflow, begin/write/finish exceptions or failures, active replacement, cancel/reset/destructor idempotence, and allocation denial all fail closed | `184a0fb` |
| Task 9C review | Callback re-entry cannot resurrect an already aborted stream as successful | Actual begin/write/finish callbacks that called `cancel()` failed 4/82 checks: all three outer operations returned `OK`, and finish overwrote `FAILED` with `COMPLETE` | Focused `-Werror` and ASan/UBSan pass 82 checks; an in-progress callback guard records lifecycle re-entry, transfers abort ownership once, and requires unchanged active ownership before an outer operation can report success | `72e7cab` |
| Task 9C re-review | Reentrant cancellation cannot free an external sink while its callback is still executing | Begin/write/finish callbacks that continued after nested `cancel()` failed 3/88 checks because external abort ran on the callback stack | Focused `-Werror` and ASan/UBSan pass 88 checks; callback-time cancellation is recorded only, then the outer operation returns from external code before invoking abort exactly once | `8af1ef7` |
| Task 9C route prep | Route body parsing is explicit rather than inferred from a nonzero cap | Focused access-policy compile exited 1 with undeclared `RouteBodyKind` and `routeBodyPolicyIsValid`; the old gate mapped every nonzero body to form parsing | Access policy passes 294 checks and request gate 2,201; all 18 existing routes retain exact behavior while compile-time validation admits only bounded POST form/stream metadata and rejects invalid cap/kind combinations | `39e3a2c` |
| Task 9C server plumbing | An authenticated binary route can stream to one lifecycle-owned sink without bypassing bounded responses | Focused transaction compile exited 1 because `rejectBody()` did not exist; the runtime contract also failed on missing consumer ownership/configuration and gate-before-begin ordering | Transaction passes 837 checks; runtime contract passes; pinned firmware compiles at 1,136,064 bytes (86%) and 72,572 bytes globals. The sink starts only after central gate and request policy acceptance, each 256-byte chunk is written before drain, finish precedes dispatch, and all timeout/socket/consumer/client cleanup paths cancel | `f143a0e` |
| Task 9D | A pending image carries its exact signed authorization across reboot | Focused update-policy compile exited 1 with missing `PendingUpdateReceipt`, `kPendingReceiptBytes`, and receipt codec APIs | Focused `-Werror` and ASan/UBSan pass 602 checks; one canonical 480-byte CRC image retains exact manifest/signature, re-authorizes after reboot, rejects truncation, and detects a bit flip at every byte offset | `bf1a110` |
| Task 9D runtime | ESP32 OTA uses only an injected public anchor, inactive partition streaming, signed pending re-authorization, and bootloader rollback APIs | New `bash scripts/check_firmware_update_runtime.sh` exited 1, `missing signed update runtime: lib/FirmwareUpdateRuntime.h` | Runtime contract passes and the pinned target compiles: default empty trust anchor rejects updates; P-256/SHA-256 verifies canonical metadata, OTA writes/hash checks before boot selection, signed receipt re-authorizes pending boot, monotonic sequence advances before health confirmation, and rollback APIs handle failure. Unlinked runtime snapshot is 1,137,180 bytes (86%), globals 72,572 | `281464e` |
| Task 9D review | The release trust anchor accepts only NIST P-256, not another 256-bit ECDSA curve | Runtime contract exited 1 on missing `MBEDTLS_ECP_DP_SECP256R1`; review showed bit length plus ECDSA also admitted secp256k1 | Runtime inspects the parsed EC group ID and requires `MBEDTLS_ECP_DP_SECP256R1`; contract and pinned target compile pass at 1,137,180 bytes (86%) | `d8212c5` |
| Task 9D quality fix | A pending boot confirms only the exact signed running bytes and cannot succeed while runtime state is unavailable | Runtime contract exited 1 on missing `esp_partition_read`; review showed receipt authorization did not hash the running partition, unready confirmation returned true, and receipt cleanup followed irreversible mark-valid | Runtime re-hashes exactly signed `artifactSize` bytes with constant-time SHA-256 comparison before pending health; pending state is latched before NVS reads; unready pending boot rolls back; receipt remove/readback precedes mark-valid. Contract and target compile pass at 1,137,212 bytes (86%) | `b92863d` |
| Task 9D re-review | Pending boot state is known before any sequence NVS failure can return | Runtime contract exited 1, `pending OTA state must latch before fallible sequence storage` | `esp_ota_get_state_partition` and `_pendingVerify` now precede `_sequenceStore.load()`; corrupt/unreadable sequence state therefore leaves rollback active. Contract and target compile pass | `cce4ffa` |
| Task 9B | Binary request bodies reach an authorized consumer only through fixed, explicitly drained chunks | Replacing the prior STREAM 501 expectation with the safe stream contract made the focused `-Werror` compile exit 1: `RequestBodyChunk`, `pendingStreamChunk()`, `drainStreamChunk()`, `kStreamBodyLimit`, and `kStreamBodyDeadlineMs` were missing. A later zero-length probe failed 2/225, and a direct late-drain probe failed 2/830. | Focused `-Werror` and ASan/UBSan runs pass 830 checks; the full host suite passes. STREAM requires explicit nonzero length and exact `application/octet-stream`, enforces route and 1,310,720-byte global caps, exposes at most 256 read-only bytes with backpressure, zeroes drained/reset/aborted bytes, and uses one wrap-safe 120-second absolute deadline while forms remain at 1,500 ms. | `da220d1` |
| Task 9 integration | Only an administrator can authorize metadata or stream the exact signed image; a pending image confirms only after storage, transport, and Web-security startup checks | Access-policy RED expected 21 routes but production exposed 18; UI/runtime contracts failed on missing update routes, stream consumer injection, boot confirmation, and rollback calls. The pending-sequence guard then exited 1 on missing `allowFirstInitialization && !_pendingVerify`; the first target compile exposed an unqualified Base64 decoder and exited 1. | Access policy passes 318 checks, request gate 2,216, runtime/UI/security contracts pass, and every host executable passes. Exact `bash scripts/run_quality_gate.sh` at `110e577` with Arduino CLI 1.4.1/core 3.3.7 reports 1,156,036 bytes (88%) and 73,540 bytes globals; its warning audit permits pinned dependency warnings but rejects project/compiler warnings. Pending OTA cannot initialize missing anti-replay state. | `7f89d6a` |
| Task 9 integration critical fix | Arduino framework startup cannot auto-confirm a pending image before product health checks | Independent review of pinned core 3.3.7 found its weak `verifyRollbackLater() == false` and `verifyOta() == true` would mark the image valid inside `initArduino()` before `setup()`; the new runtime contract exited 1 on the missing strong deferral override | Sketch provides a strong `extern "C" verifyRollbackLater()` returning true. Exact clean quality gate at `475ebbb` verifies the ELF symbol type is `T`, passes all host/static/stress/TSan/build/SBOM stages, reports 1,156,044 bytes (88%), 73,540 bytes globals, and artifact SHA-256 `27ee999b7f73a7928bcaabcedc7b0fc8de855a8b904201ef40f25844a5572b2f` | `475ebbb` |
| Task 9 release evidence | Reproducible candidates bind canonical manifest, artifact, SBOM, trust anchor, external signature, and fail-closed browser/HIL evidence inputs | New `bash test/tooling/test_release_contract.sh` exited 1 on missing `scripts/package_release.sh`; after initial implementation it exited 1 because the SBOM did not bind `trust_anchor_sha256`. The first anchored target build then exited 1 on a broken quoted compiler definition | Release contract and full gate pass. An empty-anchor unsigned candidate is explicitly locked. A temporary P-256 test anchor rebuilt the clean `041e157` candidate at 1,157,192 bytes (88%), artifact 1,157,344 bytes/SHA-256 `fffecc03f5bdbd0ad3692559b656295988b354dd0c63c557088a8c0740b5f20b`; the external signer produced bounded DER, package verification returned `Verified OK`, and every bundle checksum passed. Browser/HIL validators still refuse PASS without retained external evidence. | `041e157` |
| Task 9 release review | Signing cannot legitimize a modified candidate or race mutable source files; browser/HIL summaries cannot self-attest | Independent review found the signer accepted arbitrary mutable bundles, skipped the original checksums/cross-bindings, and refreshed checksums after signing; the new release contract first exited 1 on missing provenance guards. Review also showed hand-written all-true browser/HIL JSON could pass and CI could rebuild twice. | Signing is restricted to a clean same-SHA candidate under `build/release`, rejects symlinks/extra files, requires an out-of-band checksum digest, verifies every binding, signs a private snapshot, re-verifies, and emits a separate read-only bundle. Clean anchored `f420f99` artifact is 1,157,344 bytes/SHA-256 `699144adc4449daf6ca7283f953f8b34b8a2248081bec41d65d0f8be495002f3`; source stays unsigned, signed checksums and OpenSSL verification pass. Browser/HIL require canonical raw-evidence manifests and detached signatures from public-key hashes reviewed into config; empty defaults fail. CI passes the same anchor to its first/only build. | `f420f99` |
