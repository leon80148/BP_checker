# Clinic-Readiness Review: Iteration 0

**Revision:** `e63d9a08b2636b713a1fe138006d2f1fa886d8df`

**Reviewer:** Independent product/scoring review

**Raw score:** **19/100**

**Effective score:** **19/100 — BLOCKED**

## Baseline Score

| Category | Score | Current evidence and decisive gaps |
|---|---:|---|
| Measurement / protocol | 3/20 | A01/A04/A06: named HBP path plus burst/overflow rejection tests. The 30 ms idle flush prevents credit for arbitrary fragmentation; the parser also discards official metadata. |
| Privacy / security | 1/18 | B10: existing mutation CSRF/Host policy only. Shared permanent AP, optional first credential, anonymous sensitive GETs, raw ID/log exposure, and unbounded pre-handler parsing remain. |
| Reliability / concurrency | 4/15 | C02/C08/C11/C12: async scanning, bounded frame, status surface, and CDC-open retry. Callback/main races and blocking time lookup remain. |
| Persistence | 4/10 | D04/D06/D08/D10: clean migration/ring/clear/CSV evidence. Multi-write metadata has no checksum or cut-point proof. |
| UX / clinical safety | 2/12 | E01/E12: staff-oriented dashboard and destructive confirmation. Diagnostic labels, stale/quality state, role separation, and browser evidence remain. |
| Tests | 3/10 | F01/F02/F05: 175 deterministic core/framing/loss checks. No real-device, race, power-cut, browser, or soak evidence. |
| Build / release | 1/7 | G02: normalized-name target probe compiles. Canonical build fails and dependency/SBOM/CI/artifact/update gates are absent. |
| Docs / operations | 1/8 | H05: basic troubleshooting guidance. No single reference board/power budget, secure deployment, USB2/function-32, retention/matrix, or EOL evidence. |

## Hard Caps and P1 Findings

The baseline has multiple P1s and therefore cannot release:

- permissive format-5 acceptance and discarded monitor error/movement metadata
- anonymous health/raw/subject-ID exposure and unauthenticated first claim
- permanent shared AP and unsafe Wi-Fi credential lifecycle
- callback/main USB races and blocking time lookup
- torn persistence metadata with no exhaustive recovery proof
- diagnostic `normal/abnormal` presentation and ambiguous stale state
- canonical case-sensitive build failure
- no HBP-9030 device-in-loop acceptance or transport/persistence soak

Unauthenticated access to the current identifiable raw frame activates the 69-point privacy cap; deliberate de-identification itself does not. The P1 cap is 89. The raw score remains lower than either cap.

## Reproducible Evidence

Every awarded atomic ID, exact command, result, toolchain, SHA, and reviewer is recorded in `docs/reviews/EVIDENCE_MANIFEST.md`. Key results are:

- `bash scripts/run_host_tests.sh` — 175 checks pass.
- `bash scripts/check_ui_markup.sh` — passes.
- `arduino-cli compile -b esp32:esp32:esp32s3 --board-options USBMode=default .` — exit 1 because the directory and sketch filename differ by case.
- The manifest's clean temporary normalized-name probe compiles at 82% flash and 14% globals; this earns G02 but not canonical-build G01.

This score is intentionally evidence-strict. It measures release proof, not the percentage of source code that exists.
